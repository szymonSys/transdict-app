import React, { useEffect } from "react";
import { getTranslationsByIds } from "../services/transdict-API/requests";

export default function PrivateContent({ auth }) {
  return (
    <>
      <p>private</p>
    </>
  );
}
